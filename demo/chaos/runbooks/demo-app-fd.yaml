alert_pattern: 'demo-app FD exhausted'
content: "Step 1: check_processes on host localhost. Step 2: ssh_execute on host localhost: pkill -f bash (kill the fd holder process). Step 3: finish() with summary."